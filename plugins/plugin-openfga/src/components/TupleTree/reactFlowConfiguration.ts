import { ResourceNode } from './NodeTypes/ResourceNode';
import { RoleNode } from './NodeTypes/RoleNode';
import { RootNode } from './NodeTypes/RootNode';

export const NODE_TYPES = {
  resourceNode: ResourceNode,
  roleNode: RoleNode,
  rootNode: RootNode,
};

export const NODE_HEIGHT = 40;
export const NODE_WIDTH = 40;
